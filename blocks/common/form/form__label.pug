-
	BEMPUG.beforeParse[ 'label' ] = function( block ) {

		if ( block.isElement && typeof block.data.t === 'undefined' )
			block.data.t = ( BEMPUG.getCurrentBlock() === 'form' ? 'label' : 'span' );

	};